version: '3.8'
services:
  appjsfile-service:
    # This names the final image pushed to Docker Hub
    image: $DOCKER_USER/appjsfile:latest 
    # Points to your existing Dockerfile
    build:
      context: .
      dockerfile: appjs.Dockerfile
